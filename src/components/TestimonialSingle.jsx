const TestimonialSingle = ({ testimonialProps }) => {
    const { name, image, jobTitle, feedback, rating } = testimonialProps

    return (
        <div className="w-full">

        </div>
    )
}

export default TestimonialSingle